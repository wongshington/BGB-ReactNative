import { StyleSheet } from "react-native";

export const gridStyles = StyleSheet.create({
  scrollView: {
    marginTop: 20,
  },

  gridContainer: {
    flexDirection: "row",
    flexWrap: "wrap",
    margin: -10,
    justifyContent: "center",
  },
});
