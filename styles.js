import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  // you can put this into another file and import it in here
  container: {
    flex: 1,
    // backgroundColor: "#fff",
    // color: "red",
    // alignItems: "center",
    // justifyContent: "center",
  },
  red: {
    color: "red",
  },
});
